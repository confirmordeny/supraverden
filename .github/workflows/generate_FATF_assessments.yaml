name: Generate FATF Assessment Table

on:
  push:
    paths:
      - 'data/**'
  workflow_dispatch:

# ADD THIS SECTION:
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    # ... rest of your code ...
